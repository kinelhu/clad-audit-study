# Secrets
.env
.env.*
!.env.example

# Python / R environments
.venv/
__pycache__/
*.pyc
renv/library/
renv/staging/
.Rhistory
.RData

# Generated by analysis/run_all.R — every table and figure in the paper is rebuilt from
# data/analysis_frame.csv, so they are outputs, not inputs.
analysis/tables/
analysis/figures/

# Retrieved full text is licensed and cannot be redistributed (see README).
data/fulltext/
data/raw/

# 13_export_figures.R writes the submission figures here. Regenerable, and a publication/ path in a
# repository that deliberately excludes the manuscript reads as a mistake.
publication/

.DS_Store
